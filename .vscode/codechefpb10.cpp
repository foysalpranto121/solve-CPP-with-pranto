#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int x, n;
        cin >> x >> n;

        int score = (x * n) / 10;
        cout << score << endl;
    }

    return 0;
}
